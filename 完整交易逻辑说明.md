# 完整交易逻辑说明 - 从信号计算到开单

**版本**: V8.0 + 简化微观结构
**创建时间**: 2026-01-22

---

## 📋 完整决策流程图

```
┌─────────────────────────────────────────────────────┐
│  第0层: 数据收集 (每4小时)                           │
│  - Binance实时价格                                  │
│  - 计算技术指标 (EMA, 张力, 加速度, 量能)            │
│  - 计算微观结构指标 (波动率, 流动性, 加速度)         │
└─────────────────────────────────────────────────────┘
                        ↓
┌─────────────────────────────────────────────────────┐
│  第1层: V8.0突变检测器 (测速仪)                     │
│  输入: EMA, 量能, 加速度                             │
│  计算: V8_Score = 0.5×EMA突变 + 0.3×量能突变 + 0.2×基础量能 │
│  触发: V8_Score >= 0.7                              │
└─────────────────────────────────────────────────────┘
                        ↓ (如果V8_Score >= 0.7)
┌─────────────────────────────────────────────────────┐
│  第2层: Singularity信号分类                         │
│  - BEARISH_SINGULARITY (系统看空)                   │
│  - BULLISH_SINGULARITY (系统看涨)                   │
│  - OSCILLATION (震荡)                               │
│  - LOW_OSCILLATION (低位震荡)                       │
└─────────────────────────────────────────────────────┘
                        ↓
┌─────────────────────────────────────────────────────┐
│  第3层: 简化微观结构过滤器 (地图+仪表盘)            │
│                                                     │
│  检测A: 负Gamma陷阱 (闪崩风险)                      │
│    IF 波动率>80% AND 加速度<-0.2 AND 流动性<0.3     │
│    THEN → 强制SKIP (跳过所有交易)                   │
│                                                     │
│  检测B: Vanna挤压 (暴涨机会)                        │
│    IF 波动率<30% AND 放量滞涨 AND 流动性>0.7        │
│    THEN → 加大仓位 (150% vs 100%)                   │
│                                                     │
│  检测C: 正常市场                                    │
│    THEN → 标准仓位 (100%)                           │
└─────────────────────────────────────────────────────┘
                        ↓
┌─────────────────────────────────────────────────────┐
│  第4层: V8.0反向策略 (核心交易逻辑)                 │
│                                                     │
│  IF 信号类型 == BEARISH_SINGULARITY:                │
│    → 做多 (系统看空，我们反向抄底)                  │
│                                                     │
│  IF 信号类型 == BULLISH_SINGULARITY:                │
│    → 做空 (系统看涨，我们反向逃顶)                  │
│                                                     │
│  IF 信号类型 == OSCILLATION:                        │
│    → 不交易 (震荡市避免磨损)                        │
│                                                     │
│  IF 信号类型 == LOW_OSCILLATION:                   │
│    → 做多 (低位震荡，做多反弹)                      │
└─────────────────────────────────────────────────────┘
                        ↓
┌─────────────────────────────────────────────────────┐
│  第5层: 仓位管理                                    │
│                                                     │
│  基础仓位: 100% (1x杠杆)                           │
│  黄金信号: 150% (1.5x杠杆)                          │
│  黑天鹅: 250% (2.5x杠杆)                            │
│  Vanna增强: 额外+50%                                │
│  负Gamma过滤: 强制0%                                │
└─────────────────────────────────────────────────────┘
                        ↓
┌─────────────────────────────────────────────────────┐
│  第6层: 风险管理                                    │
│                                                     │
│  - 止损: -3% (触发平仓)                             │
│  - 止盈: +10% (部分平仓)                            │
│  - 追踪止损: 保护50%利润                            │
│  - 强制平仓: 负Gamma陷阱触发时                       │
└─────────────────────────────────────────────────────┘
                        ↓
                      开单
```

---

## 🔢 详细计算逻辑

### 第1层: V8.0评分计算

```python
def calculate_v8_score(df):
    """
    V8.0突变检测器 - 测速仪
    不关心在哪里，只关心以多快的速度冲向哪里
    """

    # Component 1: EMA突变 (50%权重)
    # 价格偏离EMA的程度
    df['Delta_EMA'] = (df['收盘价'] - df['EMA']) / df['EMA']
    df['Delta_EMA_Abs'] = abs(df['Delta_EMA'])
    score_ema = normalize_clipped(df['Delta_EMA_Abs'], cap=0.3) * 0.5

    # Component 2: 量能突变 (30%权重)
    # 当前量能 vs 平均量能
    df['Delta_Vol'] = (df['量能比率'] - df['量能比率'].rolling(20).mean())
    score_vol = normalize_clipped(df['Delta_Vol'], cap=0.5) * 0.3

    # Component 3: 基础量能 (20%权重)
    # 绝对量能水平
    score_base = normalize_clipped(df['量能比率'], cap=2.0) * 0.2

    # 总分
    df['V8_Score'] = score_ema + score_vol + score_base

    return df

def normalize_clipped(series, cap):
    """标准化并截断到[0,1]"""
    return (series.abs() / cap).clip(0, 1)
```

**触发阈值**: V8_Score >= 0.7

**含义**: 市场正在快速变化（要么突破要么反转）

---

### 第2层: Singularity信号分类

```python
def classify_singularity(df_row):
    """
    奇点信号分类
    """

    tension = df_row['张力']  # 价格偏离度
    acceleration = df_row['加速度']  # 动量变化
    volume_ratio = df_row['量能比率']  # 量能水平

    # BEARISH_SINGULARITY: 系统极度看空
    # 张力<-0.7 (价格远低于均线)
    # 加速度<-0.2 (下跌加速)
    if tension < -0.7 and acceleration < -0.2:
        return 'BEARISH_SINGULARITY'

    # BULLISH_SINGULARITY: 系统极度看涨
    # 张力>0.7 (价格远高于均线)
    # 加速度>0.2 (上涨加速)
    if tension > 0.7 and acceleration > 0.2:
        return 'BULLISH_SINGULARITY'

    # LOW_OSCILLATION: 低位震荡
    # -0.3 < 张力 < 0.3
    # 量能比率 < 0.5 (低量能)
    if abs(tension) < 0.3 and volume_ratio < 0.5:
        return 'LOW_OSCILLATION'

    # OSCILLATION: 震荡市
    # 其他情况
    return 'OSCILLATION'
```

---

### 第3层: 简化微观结构过滤

```python
def simplified_microstructure_filter(price_history, volume_history):
    """
    简化微观结构过滤器 - 用现有数据proxy期权指标
    """

    # ===== 计算Proxy指标 =====

    # Proxy 1: 历史波动率 (→ IV)
    returns = pd.Series(price_history).pct_change().dropna()
    volatility = returns.std() * np.sqrt(252)  # 年化

    # Proxy 2: 价格加速度 (→ GEX)
    if len(price_history) >= 3:
        change1 = (price_history[-1] - price_history[-2]) / price_history[-2]
        change2 = (price_history[-2] - price_history[-3]) / price_history[-3]
        acceleration = change1 - change2
    else:
        acceleration = 0

    # Proxy 3: 成交量激增 (→ CVD)
    avg_volume = np.mean(volume_history[-20:])
    volume_surge = volume_history[-1] > avg_volume * 1.5

    # Proxy 4: 流动性评分 (→ Order Book Depth)
    liquidity = (min(volume_history[-1] / 1000000, 1.0) +
                 max(1 - abs(acceleration) / 0.05, 0)) / 2

    # ===== 检测负Gamma陷阱 =====
    is_crash_risk = False
    crash_confidence = 0

    if volatility > 0.8:  # 高波动
        crash_confidence += 0.4
    if acceleration < -0.2:  # 急跌
        crash_confidence += 0.4
    if liquidity < 0.3:  # 低流动性
        crash_confidence += 0.2

    is_crash_risk = (crash_confidence >= 0.5)

    # ===== 检测Vanna挤压 =====
    is_squeeze = False
    squeeze_confidence = 0

    if volatility < 0.3:  # 低波动 (IV便宜)
        squeeze_confidence += 0.3
    if volume_surge and abs(acceleration) < 0.1:  # 放量滞涨 (吸筹)
        squeeze_confidence += 0.4
    if liquidity > 0.7:  # 高流动性
        squeeze_confidence += 0.3

    is_squeeze = (squeeze_confidence >= 0.6)

    return {
        'is_crash_risk': is_crash_risk,
        'is_squeeze': is_squeeze,
        'liquidity': liquidity,
        'volatility': volatility
    }
```

---

### 第4层: V8.0反向策略决策

```python
def v8_reverse_strategy(signal_type, microstructure_filter):
    """
    V8.0反向策略 - 核心交易逻辑

    口诀: "系统看空我做多，系统看涨我做空，震荡观望不操作"
    """

    # ===== 强制过滤器 (负Gamma陷阱) =====
    if microstructure_filter['is_crash_risk']:
        return {
            'action': 'SKIP',
            'reason': '负Gamma陷阱 - 闪崩风险',
            'position_size': 0
        }

    # ===== 信号类型决策 =====

    if signal_type == 'BEARISH_SINGULARITY':
        # 系统极度看空 → 我们反向做多 (抄底)
        base_position = 1.0

    elif signal_type == 'BULLISH_SINGULARITY':
        # 系统极度看涨 → 我们反向做空 (逃顶)
        base_position = -1.0

    elif signal_type == 'LOW_OSCILLATION':
        # 低位震荡 → 做多 (反弹)
        base_position = 1.0

    elif signal_type == 'OSCILLATION':
        # 震荡市 → 不交易
        return {
            'action': 'WAIT',
            'reason': '震荡市 - 避免磨损',
            'position_size': 0
        }

    # ===== 微观结构调整 =====

    final_position = base_position  # 基础仓位

    # Vanna挤压增强
    if microstructure_filter['is_squeeze']:
        if base_position > 0:  # 做多
            final_position = base_position * 1.5  # 增加50%仓位
        # 做空时不增强 (避免squeeze风险)

    # 黄金信号增强
    if is_golden_signal():  # 需要额外判断
        final_position = final_position * 1.5

    # 黑天鹅增强
    if is_black_swan():  # 需要额外判断
        final_position = final_position * 2.5

    return {
        'action': 'LONG' if final_position > 0 else 'SHORT',
        'position_size': abs(final_position),
        'reason': f'{signal_type} + 微观结构调整'
    }
```

---

### 第5层: 仓位管理

```python
def calculate_position_size(base_size, account_balance, risk_per_trade=0.02):
    """
    计算实际开单仓位

    base_size: 策略建议的杠杆倍数 (1.0, 1.5, 2.5等)
    account_balance: 账户余额
    risk_per_trade: 单笔风险 (2%)
    """

    # 基础仓位
    base_position_value = account_balance * base_size

    # 风险调整
    # 假设止损-3%，仓位不应超过 (2% / 3%) = 66.7%
    max_position_by_risk = account_balance * (risk_per_trade / 0.03)

    # 取较小值
    actual_position = min(base_position_value, max_position_by_risk)

    return actual_position
```

**示例**:
- 账户余额: $10,000
- 策略建议: 150%仓位 (黑天鹅信号)
- 计算得出: $10,000 × 1.5 = $15,000
- 风险上限: $10,000 × (2% / 3%) = $6,667
- **实际开单**: $6,667 (风险控制优先)

---

### 第6层: 风险管理

```python
def risk_management(position, entry_price, current_price, microstructure_filter):
    """
    风险管理 - 动态止损止盈
    """

    pnl_ratio = (current_price - entry_price) / entry_price

    # ===== 强制平仓条件 =====

    # 1. 负Gamma陷阱触发
    if microstructure_filter['is_crash_risk']:
        return {
            'action': 'CLOSE',
            'reason': '负Gamma陷阱 - 紧急平仓'
        }

    # 2. 硬止损
    if pnl_ratio < -0.03:  # -3%
        return {
            'action': 'CLOSE',
            'reason': f'止损触发: {pnl_ratio:.2%}'
        }

    # 3. 止盈 (分批)
    if pnl_ratio > 0.10:  # +10% 平仓50%
        return {
            'action': 'PARTIAL_CLOSE',
            'ratio': 0.5,
            'reason': f'止盈触发: {pnl_ratio:.2%}'
        }

    # 4. 追踪止损
    if pnl_ratio > 0.05:  # 盈利>5%后
        trailing_stop = entry_price * 1.025  # 保护2.5%利润
        if current_price < trailing_stop:
            return {
                'action': 'CLOSE',
                'reason': f'追踪止损: {pnl_ratio:.2%}'
            }

    # 5. V8.0信号消失
    if current_v8_score < 0.35:  # 阈值的一半
        return {
            'action': 'CLOSE',
            'reason': 'V8.0信号消失'
        }

    return {'action': 'HOLD'}
```

---

## 🎯 完整决策示例

### 场景1: 理想做多机会

```
[时间] 2026-01-22 20:00:00
[价格] $95,000

第1层: V8.0评分
  EMA突变: 0.8 (价格快速上涨)
  量能突变: 0.6 (成交量放大)
  基础量能: 0.5
  → V8_Score = 0.75 ✅ 触发

第2层: 信号分类
  张力: +0.85 (价格远高于均线)
  加速度: +0.15
  → BULLISH_SINGULARITY (系统极度看涨)

第3层: 微观结构
  波动率: 25% (低)
  流动性: 0.8 (高)
  成交量: 放量但价格滞涨
  → Vanna挤压 ✅

第4层: 反向策略
  BULLISH_SINGULARITY → 做空
  但微观结构显示Vanna挤压 → 实际应该做多！
  → 这里有个冲突...

等等，让我重新检查逻辑...
```

**修正**: 我发现了一个逻辑冲突！

### 逻辑修正

**问题**: V8.0反向策略说"BULLISH_SINGULARITY → 做空"，但Vanna挤压说"低波动+放量 → 做多"。这两者矛盾！

**解决方案**: 微观结构过滤器的**优先级更高**

```python
# 正确的决策顺序
if microstructure_filter['is_crash_risk']:
    return 'SKIP'  # 最高优先级：安全第一

elif microstructure_filter['is_squeeze']:
    # 第二优先级：Vanna挤压
    # 不管Singularity是什么，都做多
    return 'LONG 150%'

else:
    # 第三优先级：V8.0反向策略
    if signal_type == 'BEARISH_SINGULARITY':
        return 'LONG 100%'
    elif signal_type == 'BULLISH_SINGULARITY':
        return 'SHORT 100%'
    # ...
```

**原因**: Vanna挤压意味着"低波动+吸筹"，这是比Singularity更高级别的市场状态。

---

## 📊 最终决策表

| Singularity | 微观结构 | 最终动作 | 仓位 | 说明 |
|------------|---------|---------|------|------|
| BEARISH | 负Gamma | **SKIP** | 0% | 安全第一 |
| BEARISH | Vanna挤压 | **LONG** | 150% | 吸筹+反向 |
| BEARISH | 正常 | **LONG** | 100% | 反向抄底 |
| BULLISH | 负Gamma | **SKIP** | 0% | 避免接飞刀 |
| BULLISH | Vanna挤压 | **LONG** | 150% | 吸筹优先 |
| BULLISH | 正常 | **SHORT** | 100% | 反向逃顶 |
| OSCILLATION | 任意 | **WAIT** | 0% | 震荡不交易 |
| LOW_OSC | 负Gamma | **SKIP** | 0% | 安全第一 |
| LOW_OSC | Vanna挤压 | **LONG** | 150% | 吸筹+做多 |
| LOW_OSC | 正常 | **LONG** | 100% | 低位做多 |

---

## ✅ 总结：核心逻辑

### 决策优先级（从高到低）

1. **安全第一**: 负Gamma陷阱 → 强制SKIP
2. **机会识别**: Vanna挤压 → 加大LONG
3. **反向交易**: BEARISH做多，BULLISH做空
4. **震荡观望**: OSCILLATION不交易

### 关键公式

```
最终决策 = 负Gamma检查? × Vanna检查? × V8.0反向策略

如果负Gamma: → SKIP (优先级最高)
否则如果Vanna挤压: → LONG 150%
否则: → V8.0反向策略 (LONG 100% 或 SHORT 100%)
```

### 核心优势

- ✅ **数学化**: 每个决策都有明确的数值阈值
- ✅ **分层过滤**: 从粗到细，逐步过滤
- ✅ **风险优先**: 安全检查优先于机会捕捉
- ✅ **自适应**: 根据市场状态调整仓位

---

**这就是完整的交易逻辑！**
